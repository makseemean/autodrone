"use strict";const menuBtn=document.querySelector(".menu-btn"),menu=document.querySelector(".menu");function onEntry(e){e.forEach(e=>{e.isIntersecting&&e.target.classList.add("element-show")})}menuBtn.addEventListener("click",()=>{menuBtn.classList.toggle("menu-btn_active"),menu.classList.toggle("menu_active"),document.body.classList.toggle("no-scroll")});let options={threshold:[.5]},observer=new IntersectionObserver(onEntry,options),animItems=document.querySelectorAll(".element-animation");for(let e of animItems)observer.observe(e);